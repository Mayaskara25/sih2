You are executing work order **W5 — Cross-Modal Optical–SAR Analysis** from PLAN.md in /home/mayaskara/projects/sih2.

BEFORE YOU START: read PLAN.md §0–§6 in full, then §7's W5 entry, then `docs/status/W1.md` (your data and its quirks) and `docs/status/W2.md` (the `benclip` encoder you will call). Do NOT read `old_files/readme.md` — PLAN.md §2.4 explains why it is fiction.

## WHY THIS WORK ORDER MATTERS
You own **R2**: *"System ingests two genuinely distinct modalities (optical + SAR) for the cross-modal task — not one image processed twice and relabeled."* PLAN.md calls this **the highest-risk rubric row**. Right now `satquery/specialists/fusion.py` is still a W0 stub returning `confidence_basis: "stub"` — so R2 is only half-met (the W0 IO layer holds up its end; the specialist does not).

The old draft's `BT_CM.generate_visual_modalities` "SAR proxy" is a `cv2.threshold` on the **optical** image. It is **deleted, not ported**. It would fail instantly on real radar, which is exactly what the hidden evaluation set contains.

## YOU OWN — create/edit ONLY these files:
```
satquery/specialists/fusion.py
tests/test_w5_*.py
docs/status/W5.md
requirements/extra-w5.txt
```

## DO NOT TOUCH
`satquery/contracts.py`, `satquery/io/**`, `satquery/runtime/**` (W0, frozen) · `satquery/controller/**` (W6, done — call it, don't modify) · `satquery/specialists/{vqa,grounding,change}.py` (W3/W4) · `app/**`, `satquery/report.py` (W7, done) · `scripts/data/**`, `data/**` (W1) · `PLAN.md` (read-only for every subagent, §5.1) · `pyproject.toml` (§5.6).

**⚠️ `satquery/adapters/benclip.py`, `train/**` and `checkpoints/**` are W2-owned and are BEING ACTIVELY REWRITTEN RIGHT NOW** by a training run in progress. **Call `benclip`'s public API; do not edit, and do not read the checkpoint files directly.** If you need a change there, STOP and report it.

## THE CHECKPOINT YOU WILL SEE IS UNDERTRAINED AND WILL BE REPLACED
`checkpoints/benclip` currently holds a deliberately undertrained model (it was trained with 7 in-batch negatives; retrieval sits at the floor). A better checkpoint lands shortly at the same path with the same format.

Consequences, and they are binding:
- **Do not hard-code, assert on, or tune to any specific benclip accuracy, label, or score.** Your tests must pass with either checkpoint.
- **Do not write a number you did not measure** (§5.9), and do not present benclip's current label quality as evidence of anything.
- This is precisely why task 4 below (the SAR-physics evidence path) exists: it must stand on its own if `benclip` underperforms out of domain.

## YOUR DATA — real optical + real SAR, already on disk and verified
```
data/bigearthnet/images/BigEarthNet-S2/<acq>/<patch>/<patch>_B0*.tif   Sentinel-2 optical, 1 band per file
data/bigearthnet/images/BigEarthNet-S1/<acq>/<patch>/<s1_name>_VV.tif  Sentinel-1 SAR (also _VH)
data/bigearthnet/targets_k10_10.json    13,630 patches: patch_id, s1_name, split, country, labels
data/rsvqa_lr/Images_LR/*.tif           772 optical 256x256 3-band
```
13,630 **paired** S1+S2 patches, 0 unpaired, full sha256 verified. `patch_id` ↔ `s1_name` is the join (**NOT** `patch_id_s1` — that column does not exist). These are genuine co-located optical+SAR pairs and are your primary test fixtures.

**⚠️ BigEarthNet stores ONE BAND PER GeoTIFF**, so `load_raster` returns `band_count=1` for a patch band file. Band stacking lives **only** in `benclip` (§4.5) — do not write your own stacker, and do not put band logic in `fusion.py`. Hand `benclip` a `RasterInput` and let it map bands.

## BHOONIDHI IS NOT AVAILABLE — substitute, and disclose
PLAN.md's W5 acceptance test names a real Bhoonidhi Cartosat + RISAT pair. **Registration is still in progress; that data does not exist locally.** Do not block on it and do not fabricate it.

- Build and test against the BigEarthNet S2+S1 pairs above.
- Additionally test the *shapes* the hidden set will present, using synthetic-but-honest inputs derived from real files: 1-band (Cartosat PAN-like), 4-band (Cartosat MSI-like), 1- and 2-band SAR (RISAT-like).
- **The Cartosat-2S 4-band order is an UNVERIFIED ASSUMPTION** written into both PLAN.md §4.5 and `io/raster.py`. Their agreement is *not* verification. Do not present it as established fact.
- Record the substitution in `docs/status/W5.md` as an open item.

## CONTRACT — frozen (`satquery.contracts.FusionResult`)
```python
run_fusion(optical_path: str, sar_path: str, query: str) -> {
    "text_response": str,
    "agreement_map_path": Optional[str],
    "overlay_path": Optional[str],
    "evidence": dict,
    "confidence": float,
    "confidence_basis": "stub"|"heuristic"|"calibrated"|"model_logprob",
}
```
- Keep the signature and key set exactly. Contract changes go through §5.5 — STOP and report.
- **Coerce every number with `float()`.** The validators reject `np.float32` deliberately, because `json.dump` does too.
- `confidence_basis` must stop being `"stub"`. Use `"heuristic"` unless you genuinely calibrate — an honest `"heuristic"` scores; a `"calibrated"` you did not calibrate is the exact drift PLAN.md §2.4 exists to prevent.

## TASKS
1. **Require two distinct, explicitly tagged modalities.** If both inputs are tagged optical, refuse with a clear message rather than silently proceeding. Note the controller (W6) already performs a validation pass — make sure the two agree and do not double-report. `resolve_modality` legitimately returns `"unknown"` for a bare 1–2-band file with no filename hint; **fail soft on `unknown`, do not hard-refuse it**, because that is what real Cartosat PAN and RISAT products look like.
2. **Real SAR preprocessing.** dB/log scaling is mandatory and already lives in `satquery.io.raster` — use it, do not reimplement. Speckle filtering (Lee / Refined Lee) only if time allows, and **if you do not implement Lee, do not mention Lee anywhere** (§2.4, §5.9 — the old code imported a Lee filter and never called it; that is the failure being corrected).
3. **Embed optical through benclip's S2 path and SAR through its S1 path** (`embed_optical` / `embed_sar` / `predict_labels`). Produce per-class predictions from each and a structured **agreement / disagreement** summary in `evidence` — this is the "joint alignment & feature fusion" the rubric names.
4. **A SAR-physics evidence path that stands alone.** Low backscatter → smooth surface / water; high backscatter and double-bounce → built-up. Simple, physically defensible, degrades gracefully on out-of-domain input. This is your insurance against benclip being weak out of domain — see the checkpoint warning above.
5. **Render an agreement map** for the W7 app, written under `runs/` (gitignored — never commit one) and returned via `agreement_map_path`.
6. `evidence` must **name both modalities explicitly** and record which file supplied each.

## THE TEST THAT MATTERS MOST
**Assert the result changes when the SAR input is swapped for a different scene, with the optical input held fixed.** This is the direct, mechanical proof that the second modality is actually being read and is not decorative — it is what separates R2 being met from R2 being claimed. A reviewer will look for exactly this. Write it as a real test, not a comment.

## RULES (PLAN.md §5)
- **No dead code** (§5.4): every function gets a live caller or a test. An imported-but-uncalled filter is a defect, not a placeholder.
- Do not import another specialist (§5.3).
- **Do not write a number you did not measure**; label placeholders `PLACEHOLDER` (§5.9).
- Tests must not download model weights unconditionally or dispatch a real model without gating. Gate on `torch.cuda.is_available()` plus the data being present, so a GPU-less clone still gets a green suite. `pytest tests/` must not pull GBs of weights.
- Do **not** write a test that asserts `confidence_basis == "stub"` for your own specialist — that breaks by construction the moment you implement it. Test contract conformance instead. (`tests/test_w0_stubs.py` has a stub-section entry for `run_fusion`; **move it to the real section** rather than deleting the assertion — that file is shared, so touch only the `run_fusion` lines.)

## GIT
- Work on the **current branch in the current working tree** (`w1-data`). Do not create or switch branches — other workstreams are active in this tree.
- **NEVER `git add -A` or `git add .`** (§5.7). Stage only paths you own, by name. A broad add previously swept another agent's in-progress files into an unrelated commit and had to be undone with a soft reset.
- Prefer not to commit; leave it to the orchestrator. `runs/` and `checkpoints/` are gitignored.

## DONE MEANS (not "code written")
1. `run_fusion` completes on a real BigEarthNet S2 optical + S1 SAR pair and returns a non-empty `evidence` dict naming both modalities.
2. **Swapping the SAR file for a different scene changes the result** — asserted in a test.
3. A two-optical-input call is rejected with a clear error; an `unknown`-modality input degrades gracefully instead of crashing.
4. Valid `FusionResult` for 1-, 2-, 3-, 4-, 12- and 14-band inputs (the W2↔W5 seam — this is where you are most likely to break someone else's code).
5. `confidence_basis` is no longer `"stub"` and is honestly labelled.
6. A fusion run through `satquery.controller.run_query` produces **≥2 entries in `models_used`** in the trace — the problem statement requires sequencing one or more models/tools, and it must be demonstrable *from the trace*, not merely true internally.
7. `uv run --no-sync pytest tests/ -q` — your tests pass AND the existing **294** still pass.

## ENVIRONMENT
```
cd /home/mayaskara/projects/sih2
uv run --no-sync pytest tests/test_w5_*.py -q
uv run --no-sync pytest tests/ -q
```
Python 3.11, torch 2.13.0+cu130, transformers **5.16.x** (a major version — use 5.x APIs, do not copy 4.x-era calls from `old_files/`). Local GPU is a GTX 1650 with **3.9 GB usable VRAM** — the old readme's "6GB" is wrong. Do not start a training run; you are inference-only.

## REPORT BACK
Files written; test results; how you proved the SAR path actually reads the SAR file; what your agreement/disagreement summary contains; what the SAR-physics fallback asserts and on what physical basis; how you handled `unknown` modality; and any contract pressure. Write `docs/status/W5.md`, recording the Bhoonidhi substitution and the unverified Cartosat band order as open items. Do not edit PLAN.md.
