You are executing work order **W0** from PLAN.md in /home/mayaskara/projects/sih2.

BEFORE YOU START: read PLAN.md sections 0–6 in full, then section 7's W0 entry.
Do NOT read old_files/readme.md — PLAN.md §2.4 explains why (it documents features that
do not exist in the code, and implementing from it is a plan violation).
You MAY read old_files/*.py for reference. Never edit them, never import them.

W0 BLOCKS EVERY OTHER WORK ORDER. Nothing else can start until this merges. Correctness
here matters more than speed — every later agent builds against what you produce.

## GOAL
Every other work order has a stable, correct foundation to build against, and nobody has
to guess an interface.

## YOU OWN (edit freely)
  pyproject.toml, .gitignore
  satquery/contracts.py
  satquery/io/**            (raster.py, modality.py)
  satquery/runtime/**       (modelpool.py)
  every __init__.py in the repo
  tests/test_w0_*.py
  docs/status/W0.md
You also CREATE the four specialist stubs (satquery/specialists/vqa.py, grounding.py,
change.py, fusion.py). Ownership of those four files transfers to W3/W4/W5 on merge —
create them, then treat them as theirs.

## READ-ONLY (never edit)
Everything else, especially PLAN.md and old_files/**.

## BRANCH
w0-foundations. The repo currently has ZERO commits — your first act is the initial commit.

## TASKS

1. **Git hygiene.** Write .gitignore per PLAN.md §5.7 — it must cover data/ (except
   data/manifests/), runs/, checkpoints/, *.safetensors, *.pt, __pycache__/, .venv/,
   old_files/__pycache__/. Make the initial commit. Note old_files/ IS committed (it's
   reference material) but its __pycache__ is not.

2. **Environment.** uv project pinned to Python 3.11 — NOT the system 3.14.7, which is too
   new for the torch stack. Deps: torch, transformers, rasterio, opencv-python, pillow,
   numpy, streamlit, pytest. Verify `import torch; torch.cuda.is_available()` returns True
   on the GTX 1650. Set HF_HOME explicitly (see §2.2a — only ~20 GB free, the HF cache
   silently accumulates old revisions).

3. **satquery/contracts.py** — encode PLAN.md §4.1 (five specialist signatures), §4.2
   (execution trace), and §4.5 (benclip band-mapping payload) as dataclasses or TypedDicts,
   plus a `validate_*` function per contract. Everyone imports these. Note confidence_basis
   is an enum of exactly: stub | heuristic | calibrated | model_logprob.

4. **satquery/io/raster.py** — the §4.4 contract. THIS IS THE HIGHEST-VALUE FILE IN THE
   REPO; take the time. It is the ONLY place a raster is ever opened. Returns a RasterInput
   dataclass (array, modality, band_count, band_names, crs, transform, is_georeferenced,
   source_path, display_rgb).

   It must correctly handle 1-band (Cartosat panchromatic, RISAT single-pol), 2-band (RISAT
   dual-pol), 3-band (PNG/JPEG benchmark inputs), 4-band (Cartosat-2S MSI), and 12–13-band
   (Sentinel-2) rasters. SAR needs dB/log scaling BEFORE any percentile stretch — a linear
   stretch of SAR intensity is unreadable.

   Why this matters: the hidden ISRO evaluation set is 1-band Cartosat PAN and 1–2-band
   RISAT (§2.3). The old code's hardcoded `src.read([1,2,3])` hard-crashes on it. Getting
   this right is the cheapest available point on the cross-modal rubric row.

5. **satquery/io/modality.py** — explicit modality tagging with §4.4's precedence order:
   (1) user selection, (2) filename/metadata heuristic, (3) band-count heuristic. Always
   report WHICH mechanism decided, so it lands in the trace. Never guess silently.

6. **satquery/runtime/modelpool.py** — §4.3 single-resident model manager. acquire(role) /
   release(), emptying the CUDA cache on release; acquiring a second heavy role
   auto-releases the first. The card is 4 GB (NOT the 6 GB old_files/readme.md claims), and
   the old models_registry.py caches Qwen2-VL and Grounding DINO in module globals
   simultaneously, which is a guaranteed OOM. No specialist may ever cache a model itself.

7. **Specialist stubs** — vqa.py (run_vqa, run_caption), grounding.py (run_grounding),
   change.py (run_change), fusion.py (run_fusion). Real signatures per §4.1, valid dummy
   payloads, confidence_basis "stub". Each raises nothing and passes the contract
   validators. These are what unlock parallel work: W6 and W7 build against them
   immediately instead of waiting on W2–W5.

8. **Every __init__.py, with all module exports pre-declared** (pointing at the stubs).
   Frozen after you. This is deliberate — it means W3/W4/W5 replacing a stub body changes
   nothing at package level, so no two agents ever edit the same __init__.py.

9. **docs/status/W0.md** — what's done, what's stubbed, anything you had to decide.

## RULES (PLAN.md §5 applies in full)
- No dead code: every function you add gets a live caller or a test in the same change (§5.4)
- No module-global model caching — that's what modelpool.py exists for (§4.3)
- No specialist imports another specialist (§5.3)
- Do not write a number you did not measure or name a technique you did not implement;
  label placeholders PLACEHOLDER (§5.9)
- If you believe a §4 contract needs changing, STOP and report it. Follow §5.5 — do not
  edit contracts.py unilaterally in a way that diverges from PLAN.md §4.

## DONE MEANS
`pytest tests/test_w0_*` passes, covering:
  - contract validators accept the stubs' output and REJECT a malformed payload
  - raster.py correctly opens a synthetic 1-, 2-, 3-, 4- and 12-band GeoTIFF plus a PNG
  - SAR dB scaling produces a visually reasonable dynamic range on a synthetic speckled image
  - modelpool: acquiring role B after role A leaves exactly ONE model resident
    (assert on torch.cuda.memory_allocated, don't eyeball it)

Not "code written" — the acceptance test passing.

## WHEN FINISHED
Write docs/status/W0.md. Do not edit PLAN.md. Report back with: the test output, anything
you had to decide that PLAN.md left open, and any contract pressure you hit.
