You are executing work order **W4 — Bi-Temporal Change Analysis** from PLAN.md in /home/mayaskara/projects/sih2.

BEFORE YOU START: read PLAN.md §0–§6 in full, then §7's W4 entry, then `docs/status/W5.md` (**your closest model — W5 solved the same shape of problem and its pattern is the one to follow**), plus `docs/status/W1.md` and `docs/status/W2.md`. Do NOT read `old_files/readme.md` — PLAN.md §2.4 explains why it is fiction.

## WHY THIS WORK ORDER MATTERS
You own **R3**: *"System ingests two dates of the same modality for bi-temporal, on a code path distinct from cross-modal."* `satquery/specialists/change.py` is currently the **last remaining W0 stub** in the project — every other specialist is real. Closing it takes the submission from 7/8 to **8/8 hard requirements**.

## SCOPE IS DELIBERATELY REDUCED — READ THIS BEFORE PLANNING
PLAN.md §7's W4 acceptance test names CDVQA and SECOND. **That data is NOT obtainable** — both are Google Drive / Baidu hosted with no direct HTTP link, and SECOND's size is unknown (`docs/status/W1.md`). **Do not attempt to download them. Do not write a data fetcher — that is W1-owned.**

You are executing PLAN.md §8's explicit fallback line instead:

> W4 and W5 built on classical CV plus `benclip` labels and **no** trained change/fusion head. **Every hard requirement R1–R8 is still met on this line**; the benchmark numbers are just weaker.

**R3 requires the capability, not a benchmark score.** A real change path that ingests two dates on its own code path satisfies it. CDVQA/SECOND would only supply numbers, and `docs/RESULTS.md` already carries an honest `PLACEHOLDER` for that row.

**W5 is your template**: classical physics + `benclip` labels, `confidence_basis: "heuristic"`, honestly labelled — and R2 is genuinely met by it. Do the same here. Do **not** train anything.

## YOU OWN — create/edit ONLY these files:
```
satquery/specialists/change.py
tests/test_w4_*.py
requirements/extra-w4.txt
docs/status/W4.md
```

## DO NOT TOUCH
`satquery/contracts.py`, `satquery/io/**`, `satquery/runtime/**` (W0, frozen) · `satquery/controller/**` (W6) · `satquery/specialists/{vqa,grounding,fusion}.py` (W3/W5) · `satquery/adapters/**`, `train/**`, `checkpoints/**` (W2) · `scripts/data/**`, `data/**` (W1) · `app/**`, `satquery/report.py` (W7) · `eval/**` (W8) · `PLAN.md` (read-only for every subagent, §5.1) · `pyproject.toml` (§5.6).

Call `benclip`'s public API; never edit it. If you need a change there, **STOP and report it**.

## CONTRACT — frozen (`satquery.contracts.ChangeResult`)
```python
run_change(image_path_t0: str, image_path_t1: str, query: str) -> {
    "text_response": str,
    "change_mask_path": Optional[str],
    "overlay_path": Optional[str],
    "metrics": dict,
    "confidence": float,
    "confidence_basis": "stub"|"heuristic"|"calibrated"|"model_logprob",
    "evidence": dict,
}
```
- Keep the signature and key set exactly. Contract changes go through §5.5 — STOP and report.
- **Coerce every number with `float()`.** The validators reject `np.float32` deliberately, because `json.dump` does too.
- `confidence_basis` must stop being `"stub"`. Use `"heuristic"` — an honest heuristic scores; a `"calibrated"` you did not calibrate is exactly the drift PLAN.md §2.4 exists to prevent.
- Write masks/overlays under `runs/` (gitignored). **Never commit one.**

## TASKS
1. **Registration — check before you warp.** If both inputs are georeferenced and already co-registered (compare CRS/transform via `satquery.io.load_raster`), **skip registration entirely**. Only fall back to feature-based alignment when they genuinely differ.
2. If you do adapt ORB/affine logic from `old_files/BT_CM.py`, you must first fix every §2.5 bug: fit the affine on the **filtered** matches (not the raw set); **null-check `estimateAffinePartial2D`** and fall back to no-warp with a low-confidence flag rather than crashing; guard empty/None ORB descriptors.
3. **Radiometric normalisation** (histogram matching) before differencing — otherwise illumination differences read as change.
4. **Change mask**: differencing + morphological cleanup, saved to disk, path returned.
5. **The part that makes this real — do not skip it.** Run `benclip.predict_labels()` on **both dates** and emit a structured **per-class delta** into `evidence` (e.g. built-up +8%, water −3%). The change answer must be grounded in an RS-adapted model's read of both scenes, not pixel arithmetic alone. This is what separates R3 being met from "differencing with a caption bolted on".
6. **Overlay render** for the W7 app.
7. **Featureless-scene handling.** Uniform water or farmland is exactly where ORB dies. Test it deliberately with a synthetic uniform+noise pair; it must report **low registration confidence, not throw**.

## THE TEST THAT MATTERS MOST
**Assert the result changes when `image_path_t1` is swapped for a different scene, with t0 held fixed** — proving both dates are actually read.

Learn from W5's mistake here: its equivalent test accepted `evidence["sar_file"] != evidence["sar_file"]` as proof, which is **trivially true for any two distinct paths** and would have passed for code that never opened the second image. The assertion was vacuous and had to be repaired. **Your swap test must rest only on DERIVED quantities** (the per-class delta, mask statistics, text_response) — never on an echoed input path.

## DATA — real, on disk, and there is no bi-temporal set
```
data/bigearthnet/images/BigEarthNet-S2/<acq>/<patch>/<patch>_B0*.tif   Sentinel-2, 1 band per file
data/bigearthnet/images/BigEarthNet-S1/<acq>/<patch>/<s1>_VV.tif       Sentinel-1 SAR
data/rsvqa_lr/Images_LR/*.tif                                          772 optical 256x256 3-band
```
**No true bi-temporal pair exists locally.** Be honest about this in tests and in `docs/status/W4.md`:
- Test the mechanics on **real** imagery (two different real scenes exercise the full path — registration, normalisation, differencing, benclip on both).
- For *change-detection correctness*, construct **synthetic-but-honest** pairs: take one real scene and apply a known modification (paste a region, shift intensity in a known window), then assert the mask recovers the region you actually changed. That gives a real correctness signal without pretending you have ground truth you do not.
- **Never describe two unrelated scenes as a "change pair"** in output, docs, or tests. Say plainly what each fixture is.

**⚠️ BigEarthNet stores ONE BAND PER GeoTIFF** (`band_count=1` per file). Band stacking lives **only** in `benclip` (§4.5) — do not write your own stacker and do not put band logic in `change.py`. Hand `benclip` a `RasterInput`.

## RULES (PLAN.md §5)
- **No dead code** (§5.4): every function gets a live caller or a test. An imported-but-uncalled filter is a defect — the old draft imported a Lee filter and never called it, which is precisely the §2.4 failure being corrected. **If you do not implement something, do not mention it anywhere.**
- Do not import another specialist (§5.3).
- **Do not write a number you did not measure**; label placeholders `PLACEHOLDER` (§5.9).
- Tests must not download weights unconditionally or dispatch a real model ungated — gate on `torch.cuda.is_available()` plus data presence so a GPU-less clone stays green. `pytest tests/` must not pull GBs.
- **Do not write a test asserting `confidence_basis == "stub"` for run_change.** `tests/test_w0_stubs.py` has a stub-section entry for it: **move it to the real section** (W5 did this for `run_fusion` — follow that edit exactly). Touch only the `run_change` lines in that shared file.

## GIT
- Work on the **current branch in the current working tree** (`w1-data`). Do not create or switch branches.
- **NEVER `git add -A` or `git add .`** (§5.7). Stage only paths you own, by name. A broad add previously swept another agent's in-progress files into an unrelated commit and had to be undone with a soft reset.
- Prefer not to commit; leave it to the orchestrator.

## DONE MEANS (not "code written")
1. `run_change` returns a valid `ChangeResult` on two real scenes, with a written mask and overlay.
2. **Swapping t1 changes the result** — asserted on derived quantities only.
3. A **synthetic known-change pair** is recovered by the mask (real correctness signal, honestly labelled).
4. A **featureless synthetic pair** (uniform + noise) does not throw and reports low registration confidence.
5. `evidence` carries a **per-class benclip delta computed from both dates**.
6. `confidence_basis` is no longer `"stub"`.
7. A change run through `satquery.controller.run_query` yields **≥2 entries in `models_used`** in the trace — the PS requires sequencing one or more models/tools and it must be demonstrable *from the trace*.
8. `uv run --no-sync pytest tests/ -q` — your tests pass AND the existing **349** still pass.

## ENVIRONMENT
```
cd /home/mayaskara/projects/sih2
uv run --no-sync pytest tests/test_w4_*.py -q
uv run --no-sync pytest tests/ -q
```
Python 3.11, torch 2.13.0+cu130, transformers **5.16.x** (a major version — use 5.x APIs, not 4.x-era calls from `old_files/`). Local GPU is a GTX 1650 with **3.9 GB usable VRAM** (the old readme's "6GB" is wrong). benclip loads in ~25 s; budget for that. Inference only — do not train.

## REPORT BACK
Files written; test results; how you proved BOTH dates are read (and that your swap test is not vacuous); what the per-class delta contains; how registration decides to skip vs warp; what the featureless case reports; exactly which fixtures are real vs synthetic; and any contract pressure. Write `docs/status/W4.md`, recording the CDVQA/SECOND substitution as a deliberate, documented decision per PLAN.md §8's fallback line. Do not edit PLAN.md.
