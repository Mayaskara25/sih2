You are executing work order **W8 — Evaluation Harness** from PLAN.md in /home/mayaskara/projects/sih2.

BEFORE YOU START: read PLAN.md §0–§6 in full, then §7's W8 entry and §3.3 (the rubric your output must map onto row-for-row), then `docs/status/W2.md`, `docs/status/W5.md`, `docs/status/W6.md` and `docs/status/W1.md`. Do NOT read `old_files/readme.md` — PLAN.md §2.4 explains why it is fiction.

## WHY THIS WORK ORDER MATTERS
Every other work order produces a capability; W8 produces the **evidence**. Its output, `docs/RESULTS.md`, is what a judge reads. Its single most important property is that it is **honest**: a `PLACEHOLDER` is a passing outcome, a guessed number is a defect (§5.9).

## YOU OWN — create/edit ONLY these files:
```
eval/**                     EXCEPT eval/routing_testset.json (W6 authored it; you SCORE it, never edit it)
requirements/extra-w8.txt
tests/test_w8_*.py
docs/status/W8.md
docs/RESULTS.md             (generated output)
```

## DO NOT TOUCH
`satquery/**` in its entirety — contracts, io, runtime (W0, frozen), controller (W6), specialists (W3/W4/W5), adapters (W2) · `train/**` and `checkpoints/**` (W2) · `scripts/data/**`, `data/**` (W1) · `app/**`, `satquery/report.py` (W7) · `eval/routing_testset.json` (W6) · `PLAN.md` (read-only for every subagent, §5.1) · `pyproject.toml` (§5.6).

**You are a measurement harness. If a metric looks bad, report the bad number — do not "fix" the thing being measured.** If you believe a specialist is buggy, write it up in `docs/status/W8.md` and report it; do not edit it.

## CURRENT GROUND TRUTH — build against this, don't re-derive it
- **W3 done**: `run_vqa`, `run_caption`, `run_grounding` are real (`confidence_basis: "model_logprob"`).
- **W5 done**: `run_fusion` is real (`confidence_basis: "heuristic"`).
- **W4 NOT done**: `run_change` is still a W0 stub returning `confidence_basis: "stub"`. `eval/cdvqa.py` must therefore emit `PLACEHOLDER`, and must do so *because it detected a stub*, not because you hardcoded it. See "Stub detection" below.
- **W2 done**: R1 numbers are measured and committed — `docs/status/benclip_before.json` and `benclip_after_v2.json` (the current model; `benclip_after.json`/`benclip_after_v1_undertrained.json` is the superseded v1).
- **W6 done**: `eval/routing_testset.json` exists with ~100 hand-written queries.
- Full suite is currently **325 passed**. Do not regress it.

## DATA ON DISK
```
data/rsvqa_lr/Images_LR/*.tif          772 optical 256x256 3-band
data/rsvqa_lr/*test*.json              RSVQA-LR test split Q/A
data/bigearthnet/…                     13,630 paired S1+S2 patches (verify.py PASS)
eval/routing_testset.json              ~100 queries with gold task labels
```
**VRSBench is NOT downloaded** and CDVQA/SECOND are **not obtainable** yet (Google Drive/Baidu hosted, size unknown — see `docs/status/W1.md`). `eval/vrsbench.py` and `eval/cdvqa.py` must be written and must degrade to `PLACEHOLDER` with a clear "data not present" reason. **Do not write a data fetcher — that is W1-owned.**

## TASKS
1. `eval/rsvqa.py` — accuracy, answer match rate, BLEU on the RSVQA-LR **test** split. This is your one harness with both real data and a real specialist, so it is the priority; make it excellent before starting the others.
2. `eval/routing.py` — accuracy + a per-task **confusion matrix** over `eval/routing_testset.json`.
3. `eval/adaptation.py` — re-runnable R1 before/after. **Do not copy the numbers**; shell out to / import `train.measure_benclip` so the table regenerates. If a checkpoint is absent, `PLACEHOLDER`.
4. `eval/vrsbench.py` — BLEU/ROUGE/CIDEr on captioning, IoU on grounding. `PLACEHOLDER` until data lands.
5. `eval/cdvqa.py` — change-VQA accuracy, mask precision vs SECOND. `PLACEHOLDER` (data absent AND specialist stubbed).
6. `eval/run_all.py` → **`docs/RESULTS.md`**: one row per §3.3 rubric line, each carrying either a real number **with its sample count and date** or an explicit `PLACEHOLDER` naming what is missing.

## THE TRAP THAT WILL BITE YOU FIRST
**RSVQA scores exact string match, and `run_vqa` returns both `Yes` and `yes`.** Lowercase-normalise and strip both sides before comparing, or you will report a spuriously low accuracy and it will look like a model failure rather than a harness bug. Report the normalisation you apply, and report raw vs normalised accuracy side by side so the effect is visible rather than hidden.

## STUB DETECTION — mechanical, not hardcoded
A harness must decide `PLACEHOLDER` by **inspecting `confidence_basis == "stub"` at runtime**, never by a constant you wrote. When W4 lands, `eval/cdvqa.py` must start producing real numbers with no code change. Write a test that asserts this: feed it a stubbed result and a non-stubbed one and check the harness switches.

## HONESTY RULES (§5.9) — this work order lives or dies on them
- **Never write a number you did not measure.** `PLACEHOLDER` always beats a guess.
- **Always carry the sample count** next to every metric. "accuracy 0.71" is not a result; "accuracy 0.71 (n=1,000)" is.
- **Report a chance/random baseline wherever one exists.** A number without its floor is not interpretable — W2 found mAP 0.4313 looks unremarkable until you know random is 0.1729.
- **Small denominators must be flagged.** If a metric rests on <30 items, say so. W2's retrieval R@1 differed by *one image* between runs and was briefly misreported as a 57% improvement; the harness should make that class of error impossible to repeat by printing counts, not just rates.
- If a metric is ill-posed on its dataset, say so in `RESULTS.md` rather than quietly reporting it as if meaningful.

## RULES (PLAN.md §5)
- **No dead code** (§5.4): every function gets a live caller or a test.
- Go through `satquery.controller.run_query` where you need a full round trip; do not import specialists directly (§5.3).
- Contracts are frozen (§5.5). Need a change? **STOP and report it.**
- Tests must not download weights unconditionally or dispatch a real model ungated — gate on `torch.cuda.is_available()` plus data presence so a GPU-less clone stays green. `pytest tests/` must not pull GBs.
- Fixed seeds and greedy decoding: two runs must produce the same numbers.

## GIT
- Work on the **current branch in the current working tree** (`w1-data`). Do not create or switch branches.
- **NEVER `git add -A` or `git add .`** (§5.7). Stage only paths you own, by name. A broad add previously swept another agent's in-progress files into an unrelated commit and had to be undone with a soft reset.
- Prefer not to commit; leave it to the orchestrator. `runs/` and `checkpoints/` are gitignored.

## DONE MEANS (not "code written")
1. `python eval/run_all.py` produces `docs/RESULTS.md` with a row per §3.3 rubric line.
2. Every row carries a real number **with sample count and date**, or an explicit `PLACEHOLDER` naming the blocker.
3. Every harness runs on a **≥100-sample** subset with no manual intervention.
4. **Two consecutive runs produce identical numbers** (assert this, don't eyeball it).
5. Stub detection is mechanical — proven by a test, not by inspection.
6. RSVQA accuracy is reported both raw and case-normalised.
7. `uv run --no-sync pytest tests/ -q` — your tests pass AND the existing **325** still pass.

## ENVIRONMENT
```
cd /home/mayaskara/projects/sih2
uv run --no-sync python eval/run_all.py
uv run --no-sync pytest tests/ -q
```
Python 3.11, torch 2.13.0+cu130, transformers **5.16.x** (a major version — use 5.x APIs, not 4.x-era calls from `old_files/`). Local GPU is a GTX 1650 with **3.9 GB usable VRAM**. Model load is tens of seconds; the benclip checkpoint alone takes ~25 s. Budget for that in any harness that loops.

## REPORT BACK
Files written; every measured number with its sample count; which rows are `PLACEHOLDER` and exactly what unblocks each; the normalisation applied to RSVQA and its effect on accuracy; proof that two runs match; and any contract pressure. Write `docs/status/W8.md`. Do not edit PLAN.md.
