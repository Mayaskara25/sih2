# W4 — Bi-Temporal Change Analysis
#
# PLAN.md §5.6: declare extra deps here, NOT in pyproject.toml.
#
# All runtime dependencies are already declared in pyproject.toml (W0-owned)
# and verified present in this environment:
#   opencv-python 5.0.0   (ORB, BFMatcher, affine warp, morphology)
#   scikit-image 0.26.0   (skimage.exposure.match_histograms, histogram
#                          matching for radiometric normalisation)
#   numpy, rasterio, Pillow — already present.
# benclip is imported via satquery.adapters.benclip (W2-owned) and is small
# enough to stay resident (§4.3 exemption). No new packages needed. This
# file exists for §5.6 compliance.
